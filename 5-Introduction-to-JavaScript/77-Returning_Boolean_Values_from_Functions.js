// You can change a if/else statement with returns of true/false into a function with === that returns true/false

function isLess(a, b) {
  // Fix this code

 return a <= b;
 }

// Change these values to test
isLess(10, 15);
isLess(15, 10);
