// The conditional operator (ternary operator) can be used as a one line if-else expression : condition ? statement-if-true : statement-if-false;

function checkEqual(a, b) {
  return a === b ? true : false;
}

checkEqual(1, 2);
