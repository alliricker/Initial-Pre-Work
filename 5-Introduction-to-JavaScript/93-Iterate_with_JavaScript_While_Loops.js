// Use a loop to run the same code multiple times. "While" loops run "while" a specific condition is true and stops if it is no longer true.

// Setup
var myArray = [];

// Only change code below this line.

var i = 0;

while (i < 5) {
  myArray.push(i);
  i++
}
