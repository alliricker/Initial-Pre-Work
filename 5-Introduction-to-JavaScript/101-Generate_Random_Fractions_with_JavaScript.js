// Random numbers create random behavior. The function Math.random() generates a random decimal number between 0 (inclusive) and almost to 1 (inclusive) - never just a one. Math.random() can return a value

function randomFraction() {

  // Only change code below this line.

  var result = 0;
  while (result === 0) {
  result = Math.random ();
  }
return result;
  // Only change code above this line.
}
