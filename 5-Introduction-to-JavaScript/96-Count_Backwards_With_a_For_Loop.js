// For loops can also count backwards if you change the initialization condition and the final-expression. i -= 2 will decrement i by 2 each loop.

// Example
var ourArray = [];

for (var i = 10; i > 0; i -= 2) {
  ourArray.push(i);
}

// Setup
var myArray = [];

// Only change code below this line.
for (var i=9; i > 0; i -=2) {
  myArray.push(i);
}
